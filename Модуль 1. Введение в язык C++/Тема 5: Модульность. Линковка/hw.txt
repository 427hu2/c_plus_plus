1   Разделите программу с функцией Sum на два файла: заголовочный и реализация. Проверьте корректность линковки.

2.  Среднее задание:
2.1 Создайте статическую библиотеку с функциями для работы с массивами (Sum, Max, Min). Подключите её к проекту.

3   Сложное задание:
3.1 Напишите динамическую библиотеку с функцией, которая считывает строки из файла и возвращает их количество. Подключите её к проекту через CMake.