1. Создайте статическую библиотеку с одной функцией и подключите её в проект через CMake.
2. Соберите стороннюю библиотеку (например, OpenSSL) из исходников и подключите её к проекту через CMake.
3. Соберите и подключите несколько сторонних библиотек (например, Boost, zlib, и SQLite) к проекту, используя CMake и ExternalProject для управления зависимостями.